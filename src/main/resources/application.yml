#set up properties
PROJECT_ID: qwiklabs-gcp-01-f5b5f0ef401f

server:
    port: ${DEFAULT_PORT:9000}

spring:
    application:
        name: app-spanner
    cloud:
        config:
            namespace: default
            enabled: true
            name: Cloud
            profile: dev, cloud, spanner
            label: master
        gcp:
            project-id: ${PROJECT_ID:qwiklabs-gcp-01-f5b5f0ef401f}
            spanner:
                 instance-id: ${INSTANCE_ID:marketplace-db}
                 database: ${DATASET_NAME:marketplace}